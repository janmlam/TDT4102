#include <iostream>

int randomWithLimits(int a, int b) {
	int rando = std::rand();
	while() {

	}

	std::cout << rando << std::endl;

}

int main() {
	randomWithLimits(1,2);
	randomWithLimits(1,2);
	randomWithLimits(1,2);
	randomWithLimits(1,2);
	randomWithLimits(1,2);
}